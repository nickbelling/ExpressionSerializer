{
  "name": "expression-serializer-ts",
  "version": "0.1.0",
  "description": "Provides TypeScript transformation tools for converting typed lambda expressions such as `(x: MyType) => x.SomeProperty > 5 && x.OtherProperty == 'Blah'` at compile-time into OData `$filter` strings such as \"SomeProperty gt 5 and OtherProperty eq 'Blah'\".",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "main": "./dist/index.js",
  "files": [
    "./dist/**.*"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "@ngtools/webpack": "^17.1.0"
  }
}
